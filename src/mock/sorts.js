// import { sort } from '../utils/sort.js';

// function generateSort(film) {
//   return Object.entries(sort).map(
//     ([sortName, sortMovie]) => ({
//       name: sortName,
//       count: sortMovie(film).length
//     })
//   );
// }

// export { generateSort };
